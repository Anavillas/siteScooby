document.addEventListener('DOMContentLoaded', function() {
    const video = document.getElementById('myVideo');
    const playButton = document.getElementById('playButton');

    playButton.addEventListener('click', function() {
        if (video.paused) {
            // Inicia a reprodução do vídeo
            video.play();
            // Ativa o som do vídeo
            video.muted = false;
            // Muda o texto do botão para "Pause"
            playButton.textContent = '||';
        } else {
            // Pausa o vídeo
            video.pause();
            // Muda o texto do botão para "Play"
            playButton.textContent = '▶';
        }
    });
});


// TEXTO

document.addEventListener('DOMContentLoaded', function() {
    const video = document.getElementById('myVideo-virus');
    const playButton = document.getElementById('playButton-virus');

    playButton.addEventListener('click', function() {
        if (video.paused) {
            // Inicia a reprodução do vídeo
            video.play();
            // Ativa o som do vídeo
            video.muted = false;
            // Muda o texto do botão para "Pause"
            playButton.textContent = '||';
        } else {
            // Pausa o vídeo
            video.pause();
            // Muda o texto do botão para "Play"
            playButton.textContent = '▶';
        }
    });
});



// VIDEO CLEOPATRA
document.addEventListener('DOMContentLoaded', function() {
    const video = document.getElementById('myVideo-cleo');
    const playButton = document.getElementById('playButton-cleo');

    playButton.addEventListener('click', function() {
        if (video.paused) {
            // Inicia a reprodução do vídeo
            video.play();
            // Ativa o som do vídeo
            video.muted = false;
            // Muda o texto do botão para "Pause"
            playButton.textContent = '||';
        } else {
            // Pausa o vídeo
            video.pause();
            // Muda o texto do botão para "Play"
            playButton.textContent = '▶';
        }
    });
});



// VIDEO RAVENCROFT
document.addEventListener('DOMContentLoaded', function() {
    const video = document.getElementById('myVideo-raven');
    const playButton = document.getElementById('playButton-raven');

    playButton.addEventListener('click', function() {
        if (video.paused) {
            // Inicia a reprodução do vídeo
            video.play();
            // Ativa o som do vídeo
            video.muted = false;
            // Muda o texto do botão para "Pause"
            playButton.textContent = '||';
        } else {
            // Pausa o vídeo
            video.pause();
            // Muda o texto do botão para "Play"
            playButton.textContent = '▶';
        }
    });
});
// VIDEO PERICLES
document.addEventListener('DOMContentLoaded', function() {
    const video = document.getElementById('myVideo-pericles');
    const playButton = document.getElementById('playButton-pericles');

    playButton.addEventListener('click', function() {
        if (video.paused) {
            // Inicia a reprodução do vídeo
            video.play();
            // Ativa o som do vídeo
            video.muted = false;
            // Muda o texto do botão para "Pause"
            playButton.textContent = '||';
        } else {
            // Pausa o vídeo
            video.pause();
            // Muda o texto do botão para "Play"
            playButton.textContent = '▶';
        }
    });
});
